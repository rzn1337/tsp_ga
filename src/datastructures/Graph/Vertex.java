package datastructures.Graph;

public class Vertex {
    private int id;
    // You can add additional properties relevant to a vertex, such as coordinates, name, etc.

    public Vertex(int id) {
        this.id = id;
    }

    public int getId() {
        return id;
    }

    // Additional getters and setters as needed for vertex properties
}
